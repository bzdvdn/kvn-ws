AC-001: default route mode — server или direct
AC-002: split tunnel по CIDR include_ranges / exclude_ranges
AC-003: routing по отдельным include_ips / exclude_ips
AC-004: DNS resolver с кешем и TTL
AC-005: routing по доменам include_domains / exclude_domains
AC-006: ordered rules engine exclude→include, first match
AC-007: server-side NAT — iptables/nftables MASQUERADE
AC-008: DNS override для full-tunnel режима
AC-009: конфигурация маршрутизации в client.yaml
AC-010: gate-тест — YouTube напрямую, корп. ресурсы через туннель

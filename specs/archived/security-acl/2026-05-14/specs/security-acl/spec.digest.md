AC-001: сервер отклоняет пакеты из запрещённых CIDR-подсетей
AC-002: сервер принимает соединения из разрешённых CIDR-подсетей
AC-003: ограничение пропускной способности для конкретного токена/пользователя
AC-004: максимальное количество одновременных сессий на токен
AC-005: проверка HTTP Origin/Referer — валидный Origin
AC-006: проверка HTTP Origin/Referer — невалидный Origin
AC-007: просмотр активных сессий через HTTP Admin API
AC-008: принудительное завершение сессии через HTTP Admin API
AC-009: клиентская аутентификация по TLS-сертификату (mTLS) — успех
AC-010: клиентская аутентификация по TLS-сертификату (mTLS) — отклонение
AC-011: Admin API возвращает 401 без токена

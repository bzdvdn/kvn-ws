AC-001: Один бинарник — go build без внешних зависимостей
AC-002: Web форма — ввод server/token/mode через браузер
AC-003: Подключение через UI — client.New().Run(ctx) внутри процесса
AC-004: Disconnect — остановка клиента из UI
AC-005: Сохранение конфига — os.UserConfigDir() + /kvn/config.yaml (YAML, совместим с kvn-client)
AC-006: Proxy mode — SOCKS5/HTTP CONNECT через UI
AC-007: Config survives restart — форма предзаполняется из YAML
AC-008: Совместимость с kvn-client — cmd/client/main.go не изменён
